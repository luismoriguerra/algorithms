import {quickSort} from './alg-sort-quick';

describe('', () => {
  it('', () => {
    expect(quickSort([4,3,2,1])).toEqual([1,2,3,4]);
    expect(quickSort([100, -40, 500, -124, 0, 21, 7])).toEqual([-124, -40, 0, 7, 21, 100, 500]);

  })
})